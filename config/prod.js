// config/prod.js — PROD environment config
// See config/sit.js for the full convention note.
//
// NOTE: this copy is unused in the dev repo — nothing here resolves to PROD.
// It exists as the template CM copies to the PROD repo root (karaoke/config/
// prod.js), alongside that repo's own creds/gtag.js. Kept in sync by hand.
window.KN_CFG = {
  env: 'PROD'
};
